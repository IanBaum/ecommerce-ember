<ul>
{{#each shoppingCart.items as |item|}}
  <li><a {{action 'showItemDetails'}}>{{item.title}}</a> -- ${{item.cost}}</li>
  {{#if itemIsClicked}}
    <img src={{item.image}}>
    <p>{{item.description}}</p>
  {{/if}}
{{/each}}
</ul>

<h3>Total: ${{shoppingCart.total}}</h3>
